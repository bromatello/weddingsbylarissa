

services:
  web-node:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: larissa-wedding-site
    image: weddingsbylarissa-node:v1.0.1
    environment:
      - PORT=5000
    ports:
      - "8080:5000"
    volumes:
      - .:/usr/src/app:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5000/"]
      interval: 30s
      timeout: 3s
      retries: 3
