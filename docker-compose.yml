services:
  web-node:
    build:
      context: .
      dockerfile: Dockerfile
    image: weddingsbylarissa:v1.0.2
    environment:
      - PORT=5000
    ports:
      - "8080:5000"
    restart: always